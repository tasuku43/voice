# E2E tests

E2E coverage should exercise the real app or demo CLI path. Current Swift tests cover the demo CLI indirectly through core use cases. Add process-level CLI tests once output contracts stabilize.
